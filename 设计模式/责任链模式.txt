请求的发送者和接受者解耦
可以控制执行顺序，只要改变链表的顺序即可
符合开闭原则和单一职责原则

责任链模式使用的链表结构，每一个链表中的储存的内容不再是数据，而是进行逻辑判断的代码块。
执行该模式时，判断每个链表数据的返回值，如果返回true则继续遍历，返回false则为处理失败

在Servlet中
由FilterChain来控制数组的遍历
FilterChain的doFilter的调用filterArrayList中的拦截器
拦截器中接受请求和FilterChain，并继续执行doFilter
由FilterChain判断数组是否已遍历结束
结束后便不再遍历直接返回
